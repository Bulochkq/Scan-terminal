/**
 * CONFIG.JS — єдине, що треба відредагувати після деплою Apps Script.
 *
 * Встав сюди URL свого веб-додатку (той, що закінчується на /exec).
 * Для другої точки — окремий деплой Cloudflare з іншим URL тут.
 */
window.APP_CONFIG = {

  // ↓↓↓ ЗАМІНИ ЦЕ ↓↓↓
  API_URL: 'https://script.google.com/macros/s/AKfycbz3UuWh-w51F7Mvzi4htAkDUDOr6kjk1N38X49KkKJGHm4Hy8DN-hyzrZewxN75XhyLcA/exec',

  // Підпис точки — видно на стартовому екрані, щоб не переплутати склади
  SITE_NAME: 'Bratislava — Sklad 1',

  APP_VERSION: '3.0.0',

  // Як часто питати сервер про зміни інших пристроїв (мс).
  // 3 секунди, як було раніше, спалювали денну квоту Apps Script.
  PING_INTERVAL_MS: 20000,

  // Скільки рядків тягнути за один запит. Має збігатися з CFG.SHEET_CHUNK на сервері.
  CHUNK_SIZE: 4000,

  // Скільки чекати на відповідь сервера, перш ніж вважати запит невдалим (мс)
  REQUEST_TIMEOUT_MS: 45000,

  // Через скільки мс після останнього натискання відправляти накопичену зміну
  BATCH_DELAY_MS: 2000,

  // Скільки разів мовчки повторити запит при тимчасовому збої
  // ("Server busy" від Apps Script, обрив Wi-Fi, таймаут).
  // Пауза між спробами: 0.7с → 1.4с → 2.8с
  MAX_RETRIES: 2
};
